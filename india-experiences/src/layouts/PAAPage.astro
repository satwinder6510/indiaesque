---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  city: string;
  citySlug: string;
  heroImage?: string;
  datePublished?: string;
  dateModified?: string;
  faq?: { question: string; answer: string }[];
  cityLinks?: { href: string; label: string }[];
  relatedPages?: { href: string; label: string }[];
  schema?: object[];
  experiences?: {
    title: string;
    href: string;
    image?: string;
    duration?: string;
    price?: string;
    rating?: number;
    reviewCount?: number;
  }[];
  featuredExperiences?: {
    title: string;
    href: string;
    image?: string;
    duration?: string;
    price?: string;
    featured?: boolean;
  }[];
}

const {
  title,
  description,
  city,
  citySlug,
  heroImage = '/images/default-hero.jpg',
  datePublished,
  dateModified,
  faq = [],
  cityLinks = [],
  relatedPages = [],
  schema = [],
  experiences = [],
  featuredExperiences = []
} = Astro.props;

// Generate mobile image path from hero image
const heroImageMobile = heroImage.replace(/(\.[^.]+)$/, '-mobile$1');

const formatDate = (dateStr?: string) => {
  if (!dateStr) return '';
  return new Date(dateStr).toLocaleDateString('en-GB', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric',
  });
};
---

<BaseLayout title={title} description={description} schema={schema} includeFooter={false}>

  <!-- HERO -->
  <div class="hero">
    <picture class="hero-picture">
      <source media="(max-width: 599px)" srcset={heroImageMobile} />
      <source media="(min-width: 600px)" srcset={heroImage} />
      <img src={heroImage} alt={`${title} - ${city}`} class="hero-img" loading="eager" />
    </picture>
    <div class="hero-overlay">
      <span class="hero-location">{city.toUpperCase()}</span>
      <span class="hero-title" role="heading" aria-level="1">{title.toUpperCase()}</span>
    </div>
  </div>

  <!-- ARTICLE -->
  <article class="article">
    <div class="article-content">
      <slot />
    </div>

    <!-- AUTHOR -->
    <div class="author">
      <div class="author-avatar"></div>
      <div class="author-info">
        <span class="author-name">Indiaesque Team</span>
        <span class="author-date">{formatDate(dateModified || datePublished)}</span>
      </div>
    </div>

    <!-- IN THIS CITY (Core Guides) -->
    {cityLinks.length > 0 && (
      <div class="internal-links">
        <h3 class="internal-links-title">More in {city}</h3>
        <ul class="internal-links-list">
          {cityLinks.slice(0, 6).map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>
    )}

    <!-- RELATED (Topic-based) -->
    {relatedPages.length > 0 && (
      <div class="internal-links related">
        <h3 class="internal-links-title">Related</h3>
        <ul class="internal-links-list">
          {relatedPages.slice(0, 6).map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>
    )}

    <!-- SHARE -->
    <div class="share">
      <span class="share-label">Share with your friends</span>
      <div class="share-icons">
        <a href="#" aria-label="Facebook">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
        </a>
        <a href="#" aria-label="Twitter">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
        </a>
        <a href="#" aria-label="Pinterest">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.373 0 0 5.372 0 12c0 5.084 3.163 9.426 7.627 11.174-.105-.949-.2-2.405.042-3.441.218-.937 1.407-5.965 1.407-5.965s-.359-.719-.359-1.782c0-1.668.967-2.914 2.171-2.914 1.023 0 1.518.769 1.518 1.69 0 1.029-.655 2.568-.994 3.995-.283 1.194.599 2.169 1.777 2.169 2.133 0 3.772-2.249 3.772-5.495 0-2.873-2.064-4.882-5.012-4.882-3.414 0-5.418 2.561-5.418 5.207 0 1.031.397 2.138.893 2.738.098.119.112.224.083.345l-.333 1.36c-.053.22-.174.267-.402.161-1.499-.698-2.436-2.889-2.436-4.649 0-3.785 2.75-7.262 7.929-7.262 4.163 0 7.398 2.967 7.398 6.931 0 4.136-2.607 7.464-6.227 7.464-1.216 0-2.359-.632-2.75-1.378l-.748 2.853c-.271 1.043-1.002 2.35-1.492 3.146C9.57 23.812 10.763 24 12 24c6.627 0 12-5.373 12-12 0-6.628-5.373-12-12-12z"/></svg>
        </a>
      </div>
    </div>

    <!-- NAV -->
    <nav class="pager">
      <a href={`/${citySlug}/`} class="pager-link">&larr; Previous</a>
      <a href={`/${citySlug}/`} class="pager-link">Next &rarr;</a>
    </nav>

    <!-- FAQ -->
    {faq.length > 0 && (
      <div class="faq">
        <h2 class="faq-title">Frequently Asked Questions</h2>
        {faq.map(item => (
          <div class="faq-item">
            <h3>{item.question}</h3>
            <p>{item.answer}</p>
          </div>
        ))}
      </div>
    )}
  </article>

  <!-- FEATURED EXPERIENCES -->
  {featuredExperiences.length > 0 && (
    <section class="featured-section">
      <div class="featured-inner">
        <h2>Featured in {city}</h2>
        <p class="featured-desc">Hand-picked experiences curated by our team.</p>

        <div class="featured-grid">
          {featuredExperiences.map(exp => (
            <a href={exp.href} target="_blank" rel="noopener" class="featured-card">
              <div class="featured-image">
                <img src={exp.image || heroImage} alt={exp.title} loading="lazy" />
                {exp.duration && <span class="featured-duration">{exp.duration}</span>}
                <span class="featured-badge">Featured</span>
              </div>
              <div class="featured-body">
                <h3 class="featured-title">{exp.title}</h3>
                <div class="featured-footer">
                  <span class="featured-price">{exp.price || 'View Details'}</span>
                  <span class="featured-cta">BOOK NOW</span>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- RELATED TOURS (Viator) -->
  {experiences.length > 0 && (
    <section class="related">
      <div class="related-inner">
        <div class="related-header">
          <h2>Related Tours</h2>
          <a href={`/${citySlug}/`}>&rarr;</a>
        </div>
        <p class="related-desc">Book these experiences in {city}. All prices shown in INR.</p>

        {experiences.slice(0, 2).map(exp => (
          <a href={exp.href} target="_blank" rel="noopener" class="holiday-card">
            <div class="holiday-image">
              <img src={exp.image || heroImage} alt={exp.title} />
            </div>
            <div class="holiday-body">
              <span class="holiday-location">{city.toUpperCase()}</span>
              <p class="holiday-title">{exp.title}</p>
              <div class="holiday-meta">
                {exp.duration && <span>{exp.duration}</span>}
                {exp.rating && exp.rating > 0 && (
                  <span class="rating">{'★'.repeat(Math.round(exp.rating))} {exp.rating.toFixed(1)}</span>
                )}
              </div>
              <div class="holiday-footer">
                <span class="holiday-price">{exp.price || 'View Details'}</span>
                <span class="holiday-arrow">&rarr;</span>
              </div>
            </div>
          </a>
        ))}
      </div>
    </section>
  )}

  <!-- NEWSLETTER -->
  <section class="newsletter">
    <img src="/images/newsletter-bg.jpg" alt="" role="presentation" class="newsletter-bg" />
    <div class="newsletter-overlay"></div>
    <div class="newsletter-content">
      <h2>STAY UP TO DATE BY SUBSCRIBING<br/>TO OUR NEWSLETTER</h2>
      <form class="newsletter-form">
        <input type="text" placeholder="FORENAME" required />
        <input type="text" placeholder="SURNAME" required />
        <input type="email" placeholder="EMAIL ADDRESS" required />
        <button type="submit">SUBSCRIBE</button>
      </form>
    </div>
  </section>

  <!-- CONTACT -->
  <section class="contact">
    <h3 class="contact-title">Contact</h3>
    <p class="contact-phone">+91 00000 00000</p>
    <p class="contact-address">Indiaesque &bull; Address Line 1<br/>Address Line 2 &bull; 110001</p>
  </section>

  <!-- FOOTER LINKS -->
  <section class="foot-links">
    <div class="foot-col">
      <h4>Destinations</h4>
      <ul>
        <li><a href="/delhi/">Delhi</a></li>
        <li><a href="/jaipur/">Jaipur</a></li>
        <li><a href="/mumbai/">Mumbai</a></li>
        <li><a href="/goa/">Goa</a></li>
        <li><a href="/varanasi/">Varanasi</a></li>
        <li><a href="/kerala/">Kerala</a></li>
      </ul>
    </div>
    <div class="foot-col">
      <h4>Collections</h4>
      <ul>
        <li><a href="#">Food Tours</a></li>
        <li><a href="#">Heritage</a></li>
        <li><a href="#">Cooking</a></li>
        <li><a href="#">Markets</a></li>
        <li><a href="#">Day Trips</a></li>
        <li><a href="#">Art & Craft</a></li>
      </ul>
    </div>
  </section>

  <!-- COPYRIGHT -->
  <footer class="copyright">
    <p>&copy; Indiaesque {new Date().getFullYear()}</p>
    <p class="credit">Designed & Built in India</p>
  </footer>

</BaseLayout>

<style>
  /* HERO */
  .hero {
    position: relative;
    width: 100%;
    min-height: 300px;
    display: flex;
    align-items: flex-end;
    overflow: hidden;
  }
  .hero-picture {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }
  .hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }
  .hero-overlay {
    position: relative;
    z-index: 1;
    width: 100%;
    padding: 60px 20px 30px;
    background: linear-gradient(to top, rgba(0,0,0,0.6) 0%, transparent 100%);
    text-align: center;
  }
  .hero-location {
    display: block;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.2em;
    color: #fff;
    margin-bottom: 12px;
  }
  .hero-title {
    font-family: 'Oswald', sans-serif;
    font-size: 24px;
    font-weight: 400;
    letter-spacing: 0.05em;
    line-height: 1.3;
    color: #fff;
    margin: 0;
  }
  @media (min-width: 600px) {
    .hero { min-height: 400px; }
    .hero-title { font-size: 32px; }
  }

  /* ARTICLE */
  .article {
    max-width: 600px;
    margin: 0 auto;
    padding: 30px 20px;
  }
  .article-content {
    font-family: 'Muli', sans-serif;
    font-size: 15px;
    line-height: 1.7;
    color: #333;
  }
  .article-content :global(h2) {
    font-family: 'Oswald', sans-serif;
    font-size: 18px;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin: 40px 0 20px;
    color: #1a1a1a;
  }
  .article-content :global(h3) {
    font-family: 'Oswald', sans-serif;
    font-size: 16px;
    font-weight: 400;
    margin: 30px 0 15px;
    color: #1a1a1a;
  }
  .article-content :global(p) {
    margin: 0 0 20px;
  }
  .article-content :global(ul),
  .article-content :global(ol) {
    margin: 0 0 20px;
    padding-left: 20px;
  }
  .article-content :global(li) {
    margin-bottom: 8px;
  }
  .article-content :global(h4) {
    font-size: 14px;
    font-weight: 600;
    margin: 24px 0 12px;
    color: #1a1a1a;
  }
  .article-content :global(strong) {
    font-weight: 600;
    color: #1a1a1a;
  }
  .article-content :global(a) {
    color: #e07060;
    text-decoration: none;
  }
  .article-content :global(a:hover) {
    text-decoration: underline;
  }
  .article-content :global(blockquote) {
    padding: 12px 20px;
    margin: 0 0 20px;
    border-left: 3px solid #2a9d8f;
    font-style: italic;
    color: #666;
  }
  .article-content :global(blockquote p) {
    margin: 0;
  }

  /* AUTHOR */
  .author {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid #e5e5e5;
  }
  .author-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: #ddd;
  }
  .author-info {
    display: flex;
    flex-direction: column;
  }
  .author-name {
    font-weight: 500;
    color: #1a1a1a;
  }
  .author-date {
    font-size: 13px;
    color: #888;
  }

  /* INTERNAL LINKS */
  .internal-links {
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid #e5e5e5;
  }
  .internal-links.related {
    margin-top: 30px;
    padding-top: 25px;
  }
  .internal-links-title {
    font-family: 'Oswald', sans-serif;
    font-size: 16px;
    font-weight: 400;
    margin: 0 0 20px;
    color: #1a1a1a;
  }
  .internal-links-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
  }
  @media (min-width: 480px) {
    .internal-links-list {
      grid-template-columns: 1fr 1fr;
    }
  }
  .internal-links-list li {
    margin: 0;
  }
  .internal-links-list a {
    display: block;
    padding: 10px 14px;
    background: #f8f8f6;
    color: #333;
    text-decoration: none;
    font-size: 14px;
    border-radius: 4px;
    transition: background-color 0.2s, color 0.2s;
  }
  .internal-links-list a:hover {
    background: #e07060;
    color: #fff;
  }

  /* SHARE */
  .share {
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid #e5e5e5;
    text-align: center;
  }
  .share-label {
    display: block;
    font-size: 12px;
    color: #888;
    margin-bottom: 15px;
  }
  .share-icons {
    display: flex;
    justify-content: center;
    gap: 20px;
  }
  .share-icons a {
    color: #333;
  }
  .share-icons a:hover {
    color: #e07060;
  }

  /* PAGER */
  .pager {
    display: flex;
    justify-content: space-between;
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid #e5e5e5;
  }
  .pager-link {
    font-size: 13px;
    color: #888;
    text-decoration: none;
  }
  .pager-link:hover {
    color: #e07060;
  }

  /* FAQ */
  .faq {
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid #e5e5e5;
  }
  .faq-title {
    font-family: 'Oswald', sans-serif;
    font-size: 18px;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin: 0 0 30px;
    color: #1a1a1a;
  }
  .faq-item {
    margin-bottom: 25px;
  }
  .faq-item h3 {
    font-size: 15px;
    font-weight: 600;
    margin: 0 0 8px;
    color: #1a1a1a;
  }
  .faq-item p {
    font-size: 14px;
    line-height: 1.6;
    color: #666;
    margin: 0;
  }

  /* FEATURED SECTION */
  .featured-section {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    padding: 48px 20px;
  }
  .featured-inner {
    max-width: 600px;
    margin: 0 auto;
  }
  .featured-section h2 {
    font-family: 'Oswald', sans-serif;
    font-size: 20px;
    font-weight: 400;
    letter-spacing: 0.05em;
    color: #ffffff;
    margin: 0 0 8px 0;
    text-align: center;
  }
  .featured-desc {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.7);
    margin: 0 0 24px;
    text-align: center;
  }
  .featured-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
  }
  @media (min-width: 480px) {
    .featured-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  .featured-card {
    display: block;
    background: #fff;
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .featured-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0,0,0,0.2);
    border-color: rgba(255, 255, 255, 0.2);
  }
  .featured-image {
    position: relative;
    aspect-ratio: 16/10;
    overflow: hidden;
  }
  .featured-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }
  .featured-card:hover .featured-image img {
    transform: scale(1.05);
  }
  .featured-duration {
    position: absolute;
    top: 10px;
    right: 10px;
    font-family: 'Muli', sans-serif;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: #ffffff;
    background: rgba(0,0,0,0.6);
    padding: 4px 8px;
  }
  .featured-badge {
    position: absolute;
    top: 10px;
    left: 10px;
    font-family: 'Muli', sans-serif;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #1a1a2e;
    background: linear-gradient(135deg, #E9CDB0 0%, #d4a574 100%);
    padding: 3px 8px;
  }
  .featured-body {
    padding: 14px;
  }
  .featured-title {
    font-family: 'Oswald', sans-serif;
    font-size: 14px;
    font-weight: 400;
    letter-spacing: 0.02em;
    color: #111827;
    margin: 0 0 10px 0;
    line-height: 1.3;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .featured-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 10px;
    border-top: 1px solid #f3f4f6;
  }
  .featured-price {
    font-family: 'Muli', sans-serif;
    font-size: 13px;
    font-weight: 600;
    color: #111827;
  }
  .featured-cta {
    font-family: 'Muli', sans-serif;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: #0d9488;
  }

  /* RELATED HOLIDAYS */
  .related {
    background: #f5f5f0;
    padding: 40px 20px;
  }
  .related-inner {
    max-width: 600px;
    margin: 0 auto;
  }
  .related-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
  }
  .related-header h2 {
    font-family: 'Oswald', sans-serif;
    font-size: 16px;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin: 0;
    color: #1a1a1a;
  }
  .related-header a {
    font-size: 18px;
    color: #1a1a1a;
    text-decoration: none;
  }
  .related-desc {
    font-size: 13px;
    color: #666;
    margin: 0 0 25px;
    line-height: 1.5;
  }

  /* HOLIDAY CARD */
  .holiday-card {
    background: #fff;
    overflow: hidden;
  }
  .holiday-image img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    display: block;
  }
  .holiday-body {
    padding: 20px;
  }
  .holiday-location {
    display: block;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: #e07060;
    margin-bottom: 10px;
  }
  .holiday-desc {
    font-size: 13px;
    line-height: 1.5;
    color: #666;
    margin: 0 0 15px;
  }
  .holiday-title {
    font-size: 14px;
    line-height: 1.4;
    color: #333;
    margin: 0 0 15px;
  }
  .holiday-meta {
    display: flex;
    gap: 15px;
    font-size: 11px;
    color: #999;
    margin-bottom: 15px;
  }
  .holiday-meta .rating {
    color: #E9CDB0;
  }
  .holiday-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 15px;
    border-top: 1px solid #e5e5e5;
  }
  .holiday-price {
    font-family: 'Oswald', sans-serif;
    font-size: 18px;
    color: #1a1a1a;
  }
  .holiday-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: #e07060;
    color: #fff;
    border-radius: 50%;
    text-decoration: none;
    font-size: 18px;
  }

  /* NEWSLETTER */
  .newsletter {
    position: relative;
    padding: 80px 24px;
    text-align: center;
    overflow: hidden;
  }
  .newsletter-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .newsletter-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
  }
  .newsletter-content {
    position: relative;
    z-index: 1;
  }
  .newsletter h2 {
    font-family: 'Oswald', sans-serif;
    font-size: 18px;
    font-weight: 400;
    letter-spacing: 0.1em;
    color: #ffffff;
    margin: 0 0 32px 0;
    line-height: 1.5;
  }
  .newsletter-form {
    max-width: 320px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .newsletter-form input {
    padding: 14px 16px;
    font-family: 'Muli', sans-serif;
    font-size: 12px;
    letter-spacing: 0.1em;
    border: 1px solid rgba(255, 255, 255, 0.3);
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    color: #ffffff;
  }
  .newsletter-form input::placeholder {
    color: rgba(255, 255, 255, 0.7);
  }
  .newsletter-form button {
    padding: 14px 16px;
    font-family: 'Muli', sans-serif;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.1em;
    border: none;
    background-color: #ffffff;
    color: #111827;
    cursor: pointer;
    transition: background-color 0.2s ease;
  }
  .newsletter-form button:hover {
    background-color: #f3f4f6;
  }
  @media (min-width: 768px) {
    .newsletter {
      padding: 80px 48px;
    }
    .newsletter h2 {
      font-size: 22px;
    }
    .newsletter-form {
      flex-direction: row;
      max-width: 700px;
      gap: 12px;
    }
    .newsletter-form input {
      flex: 1;
    }
    .newsletter-form button {
      flex: 0 0 auto;
      padding: 14px 32px;
    }
  }

  /* CONTACT */
  .contact {
    padding: 40px 20px;
    text-align: center;
    border-bottom: 1px solid #e5e5e5;
  }
  .contact-title {
    font-size: 11px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: #333;
    margin: 0 0 15px;
  }
  .contact-phone {
    font-size: 13px;
    color: #666;
    margin: 0 0 8px;
  }
  .contact-address {
    font-size: 13px;
    color: #666;
    line-height: 1.6;
    margin: 0;
  }

  /* FOOTER LINKS */
  .foot-links {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
    padding: 30px 20px;
    max-width: 400px;
    margin: 0 auto;
    text-align: center;
  }
  .foot-col {
    text-align: center;
  }
  .foot-col h4 {
    font-size: 11px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: #333;
    margin: 0 0 15px;
  }
  .foot-col ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .foot-col li {
    margin-bottom: 8px;
  }
  .foot-col a {
    font-size: 12px;
    color: #666;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .foot-col a:hover {
    color: #e07060;
  }

  /* COPYRIGHT */
  .copyright {
    padding: 30px 20px;
    text-align: center;
    border-top: 1px solid #e5e5e5;
  }
  .copyright p {
    font-size: 11px;
    color: #999;
    margin: 0 0 5px;
  }
  .credit {
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }
</style>
